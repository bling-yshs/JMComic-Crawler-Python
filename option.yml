# 插件配置
plugins:
  after_init:
    - plugin: find_update # 只下载新章插件
  after_photo:
  - plugin: img2pdf
    kwargs:
      pdf_dir: ./result/pdf/
      filename_rule: Pid
dir_rule:
  base_dir: ./result/down/
  rule: Bd_Pid